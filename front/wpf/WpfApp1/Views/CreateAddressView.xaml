<UserControl x:Class="WpfApp1.Views.CreateAddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid Background="White">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24" MinWidth="360" HorizontalAlignment="Center">
                <TextBlock Text="Создание адреса" FontSize="24" FontWeight="Bold" TextAlignment="Center"/>
                <TextBlock Text="Заполните форму, чтобы добавить новый адрес." TextAlignment="Center" TextWrapping="Wrap"/>

                <StackPanel Orientation="Vertical" Margin="0,16,0,0">
                    <TextBlock Text="Почтовый индекс"/>
                    <TextBox x:Name="PostalCodeTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Страна"/>
                    <TextBox x:Name="CountryTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Регион"/>
                    <TextBox x:Name="RegionTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Район"/>
                    <TextBox x:Name="DistrictTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Город"/>
                    <TextBox x:Name="CityTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Населённый пункт"/>
                    <TextBox x:Name="LocalityTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Улица"/>
                    <TextBox x:Name="StreetTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Дом"/>
                    <TextBox x:Name="HouseTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Корпус"/>
                    <TextBox x:Name="BuildingTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Строение"/>
                    <TextBox x:Name="StructureTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Квартира / офис"/>
                    <TextBox x:Name="ApartmentTextBox" Margin="0,0,0,8"/>

                    <TextBlock Text="Комментарий"/>
                    <TextBox x:Name="CommentTextBox" Margin="0,0,0,8" AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Очистить" Width="100" Margin="0,0,8,0" Click="OnClearClick"/>
                    <Button Content="Сохранить" Width="120" Click="OnSaveClick" IsDefault="True"/>
                </StackPanel>

                <TextBlock x:Name="FormattedAddressTextBlock"
                           Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           Foreground="Gray"
                           Visibility="Collapsed"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
