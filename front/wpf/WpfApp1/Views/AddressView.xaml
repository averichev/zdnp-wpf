<UserControl x:Class="WpfApp1.Views.AddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="350" d:DesignWidth="525">
    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="360" Margin="24">
            <TextBlock Text="Список адресов" FontSize="24" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock Text="Выберите адрес из списка или добавьте новый." TextAlignment="Center" TextWrapping="Wrap" Margin="0,8,0,16"/>
            <Button Content="Добавить адрес"
                    Width="200"
                    HorizontalAlignment="Center"
                    Click="OnAddAddressClick"/>
        </StackPanel>
    </Grid>
</UserControl>
